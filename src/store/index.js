import { createStore } from 'vuex';
import apiModule from './apiModule';

export default createStore({
  state: {
  },
  getters: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
    api: apiModule,
  },
});
